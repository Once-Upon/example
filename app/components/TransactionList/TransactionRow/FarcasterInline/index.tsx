const SMALLER_FONT_SIZE = { fontSize: "smaller" };

export default function FarcasterInline() {
  return (
    <span style={SMALLER_FONT_SIZE}>
      <i className="fc fc-farcaster" style={SMALLER_FONT_SIZE}></i>
    </span>
  );
}
